import { ArrayJoinerPipe } from './array-joiner.pipe';

describe('ArrayJoinerPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayJoinerPipe();
    expect(pipe).toBeTruthy();
  });
});
