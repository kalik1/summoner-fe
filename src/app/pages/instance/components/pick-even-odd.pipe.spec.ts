import { PickEvenOddPipe } from './pick-even-odd.pipe';

describe('PickEvenOddPipe', () => {
  it('create an instance', () => {
    const pipe = new PickEvenOddPipe();
    expect(pipe).toBeTruthy();
  });
});
